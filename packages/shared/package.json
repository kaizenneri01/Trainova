{
  "name": "@trainova/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared code and types across the monorepo",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@trainova/config": "workspace:*",
    "typescript": "^5.3.3",
    "eslint": "^8.55.0"
  }
}
